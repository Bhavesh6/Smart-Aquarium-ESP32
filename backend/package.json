{
  "name": "smart-aquarium-api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "migrate:local": "wrangler d1 migrations apply smart-aquarium-db --local",
    "migrate:remote": "wrangler d1 migrations apply smart-aquarium-db --remote"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
